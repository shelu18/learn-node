const {sum,multiply} = require ('./basics/app.js');
console.log(sum(5, 10));
console.log(multiply(5, 10));
// The above code imports the sum and multiply functions from the app.js file and uses them to perform calculation